server.port=8081

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/user_db
spring.datasource.username=postgres
spring.datasource.password=Bholu@620

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT (must match gateway secret for local demo)
app.security.secret=dev-secret-change-me-use-at-least-32-bytes
app.security.expirationMs=604800000

# Creates a local demo admin on startup when it does not already exist.
app.admin.bootstrap.enabled=true

# Internal service URLs used only by admin account creation.
services.doctor.url=http://localhost:8082
services.patient.url=http://localhost:8083
