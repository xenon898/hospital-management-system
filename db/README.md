## SQL scripts
This folder is prepared for future SQL scripts.

Recommended approach:
- Enable `spring.jpa.hibernate.ddl-auto=update` for beginner demo.
- Use seed inserts (or later replace with explicit `*.sql` scripts).

(If you want, I can generate the SQL scripts + seed data next.)

