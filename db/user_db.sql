-- user_db.sql
-- Creates sample users for User Service (roles: ADMIN, DOCTOR, PATIENT)
-- IMPORTANT: Passwords should be hashed. For a simple student demo, this script inserts a BCrypt hash.
-- If your BCrypt hash generation differs, use the backend register endpoint instead.

-- Tables will be created automatically by JPA because ddl-auto=update.
-- This script is optional.

-- Example (optional) inserts are commented out.

