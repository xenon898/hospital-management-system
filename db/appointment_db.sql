-- appointment_db.sql
-- Appointment service uses JPA ddl-auto=update, so tables are created automatically.
-- This file is for optional manual inserts if you want to pre-seed appointments.
-- Recommended flow for a beginner demo:
--  1) Book appointments via POST /api/appointments (PATIENT)
--  2) Update status via PATCH /api/appointments/{id}/status (DOCTOR)

